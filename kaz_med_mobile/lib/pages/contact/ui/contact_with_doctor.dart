import 'package:flutter/material.dart';

class ContactWithDoctor extends StatelessWidget {
  const ContactWithDoctor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
